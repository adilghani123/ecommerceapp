import { defineNuxtPlugin } from '#app';
import 'bootstrap/dist/js/bootstrap.bundle.min.js';

export default defineNuxtPlugin(() => {
  // No need to do anything here as Bootstrap JS is automatically included
  // through the import above, but you can initialize anything if needed.
});